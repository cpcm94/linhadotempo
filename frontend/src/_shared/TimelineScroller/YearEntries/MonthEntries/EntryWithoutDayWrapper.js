import styled from 'styled-components'

import Grid from '@material-ui/core/Grid'

export const EntryWithoutDayWrapper = styled(({ color, ...other }) => (
  <Grid item={true} xs={10} {...other} />
))``
